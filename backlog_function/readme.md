This backlog process file is meant to be run when changes are made to the fields gathered from "extractor-llm-jf" in order to fully capture all the historical data that has been scraped.

To run:

Google CLI must be installed.

Install the requirements.txt (google cloud packages)

log in with: gcloud auth application-default login

Run the file